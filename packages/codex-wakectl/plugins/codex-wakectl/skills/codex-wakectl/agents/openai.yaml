interface:
  display_name: "Codex Wakectl"
  short_description: "Schedule timed and conditional thread input"
  default_prompt: "Use $codex-wakectl to schedule Codex thread input after a time or condition."
policy:
  allow_implicit_invocation: true
