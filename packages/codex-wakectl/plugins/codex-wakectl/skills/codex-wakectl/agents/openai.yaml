interface:
  display_name: "Codex Wakectl"
  short_description: "Inspect, control, and wake Codex threads"
  default_prompt: "Use $codex-wakectl to inspect a Codex thread before choosing whether to wait, steer, interrupt, or wake it."
policy:
  allow_implicit_invocation: true
