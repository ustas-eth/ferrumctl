interface:
  display_name: "Codex Wakectl"
  short_description: "Send and schedule Codex thread input"
  default_prompt: "Use $codex-wakectl to send input now or when a later condition is met."
policy:
  allow_implicit_invocation: true
