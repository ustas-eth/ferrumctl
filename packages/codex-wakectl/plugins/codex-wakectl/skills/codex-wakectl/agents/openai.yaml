interface:
  display_name: "Codex Wakectl"
  short_description: "Wait and schedule Codex thread input"
  default_prompt: "Use $codex-wakectl to wait for a condition or schedule input for later delivery."
policy:
  allow_implicit_invocation: true
