interface:
  display_name: "Codex Wakectl"
  short_description: "Schedule timed and conditional thread attention"
  default_prompt: "Use $codex-wakectl to schedule Codex thread attention after a time or condition."
policy:
  allow_implicit_invocation: true
