interface:
  display_name: "Codex Readcov"
  short_description: "Measure Codex transcript read coverage"
  default_prompt: "Use $codex-readcov to inspect transcript read coverage for a Codex thread."
policy:
  allow_implicit_invocation: true
