interface:
  display_name: "Codex Threadctl"
  short_description: "Inspect and control Codex threads"
  default_prompt: "Use $codex-threadctl to discover, inspect, or immediately control a Codex thread."
policy:
  allow_implicit_invocation: true
