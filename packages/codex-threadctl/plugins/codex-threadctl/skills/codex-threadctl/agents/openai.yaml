interface:
  display_name: "Codex Threadctl"
  short_description: "Create, find, inspect, and control Codex threads"
  default_prompt: "Use $codex-threadctl to create, find, inspect, or immediately control a Codex thread."
policy:
  allow_implicit_invocation: true
